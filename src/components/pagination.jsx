/**
 * @file
 * Render the pagination object.
 */

const React = require('react');

const Pagination = React.createClass({
  render() {
    return (<h1>Pagination Template</h1>);
  }
})

module.exports = Pagination;
